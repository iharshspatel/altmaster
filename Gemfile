source "https://rubygems.org"

# Pins Jekyll + plugins to the exact versions GitHub Pages uses, so a local
# build and the CI build match what the site has always run on.
gem "github-pages", group: :jekyll_plugins

# Needed to run `jekyll serve` on Ruby 3+ (webrick left the standard library).
gem "webrick"
